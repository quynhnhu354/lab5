extends layout

block content
  .container.mt-4
    a.btn.btn-secondary.mb-3(href="/products") 🔙 Quay lại danh sách sản phẩm

    .row
      .col-md-6
        img.img-fluid(src=product.imageUrl, alt=product.name)

      .col-md-6
        h1 #{product.name}
        p Giá: 
          strong #{product.price} $
        p Mô tả: #{product.description}

        form(action="/products/add-to-cart", method="post")
          input(type="hidden", name="productId", value=product._id)
          button.btn.btn-success(type="submit") 🛒 Thêm vào giỏ hàng

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")
